fn main() {
    let guess: u32 = "42".parse().expect("Not a number!");

    /*
    This is meant to be an extension of the guessing game program
    Using the u32 represents an unsigned 32 bit integer, and the
    program will check the answer given by the user. It then will
    check that it is a 32 bit integer, and if not, will display the
    error message "Not a number!"
    */
}
